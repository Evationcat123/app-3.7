# Reload Button Patch

Added a reload button (`↻`) to the browser/search bar.

- Reloads the currently displayed website with `WebView.reload()`.
- Styled consistently with the existing GO/back/forward controls.
- Includes an accessibility content description.
- No changes to navigation, search, or keyboard input behavior.
